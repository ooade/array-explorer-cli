#!/usr/bin/env node

module.exports = {
	adding: ['add items or other arrays', 'I need to add'],
	removing: ['remove items', 'I need to remove'],
	find: ['find items', "I'm trying to find"],
	single: ['one item', 'I need to find'],
	many: ['one or many items', 'I need to find'],
	iterate: ['walk over items', 'I need to iterate by'],
	string: ['return a string', 'I need to'],
	ordering: ['order an array', 'I need to'],
	other: ['something else', 'I need to']
}
